## Packages
lucide-react | Icons for the browser UI

## Notes
WebSocket connects to `/ws` relative to the current host with `ws:` or `wss:` protocol.
Expects backend to send `frame` events with base64 JPEG data.
Expects backend to handle `wsClientMessageSchema` payloads for interaction.
